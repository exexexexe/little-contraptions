node_modules/
.DS_Store

# Secrets. Never commit these — the hub reads keys from the environment
# only, and Railway holds the production copies.
.env
.env.*
!.env.example

# Local SQLite for the shared-storage toys. Production lives on the
# Railway volume at /data, never in the repo.
.data/
