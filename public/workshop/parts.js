/* parts.js — Chapter 1: Workshop Basics.
 *
 * The five classic parts, registered into the engine. Phase 2 fills these in;
 * the file exists from Phase 0 so the page's script order is the real one from
 * the start rather than something rearranged later.
 */
(function () {
  'use strict';
  if (!window.LCContraption) return;
  window.LCContraption.defineParts({});
})();
